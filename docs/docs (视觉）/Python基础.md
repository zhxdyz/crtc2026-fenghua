python第一课
1.井号表示注释，ctrl＋斜线也可以注释
2.两行代码写一行要加分号

python第二课
python开发环境的准备
1..idea表示项目配置信息
2..venv表示虚拟环境，保存项目的环境信息
python的核心语法
1.字面量与变量
（1）字面量，程序直接书写的固定值
如8，“sunshine"
a.字面量的类型
包括布尔，字符串，空值数据容错
注意：True False分别代表1和0且和None的首字母大写，字符串加引号

python第三课
1.变量：通常把经常变化的数据储存在变量中（如B站视频的播放量）
（1）变量名＝变量的值
如num＝1114.1
（2）一次性定义多个变量 base,incr＝

